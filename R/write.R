# Writers

# write
# 
# write.meta.data <- function(object, file.stem, extension, format){
#   if (!(format %in% c("csv", "table", "dta"))) {
#     stop("The writing format is not supported")
#   }
#   if (missing(extension)) {
#     if (format == "dta") 
#       extension <- ".dta"
#     if (format == "csv") 
#       extension <- ".csv"
#   }
#   
# }
# 
# write.meta.frame <- function(object, file.stem, extension, format){
#   
# }
