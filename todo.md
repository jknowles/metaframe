# To-Do

Sort out print / head / tail methods for `metaframe`. Probably make printing 
`metadata` to the console optional or eliminate it all together and create a 
new function that prints data + meta.data. 

Write up a full example documenting the airquality data set, rendering out a 
codebook

Build a RStudio Add-In for in-line documentation (allowing user to fill in the 
skeleton object).

